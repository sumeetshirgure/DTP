#include "packet.h"

int send_pkt (struct dtp_gate* gate, packet_t *packet) {

  return 0;
}

int recv_pkt (struct dtp_gate* gate, packet_t *packet) {

  return 0;
}

int make_pkt (packet_t *packet, seq_t seq, wptr_t wptr,
	      len_t len, len_t wsz, flag_t flags, const void* buffer) {

  return 0;
}
